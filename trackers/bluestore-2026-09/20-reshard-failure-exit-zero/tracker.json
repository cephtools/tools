{
 "issue": {
  "project_id": "bluestore",
  "tracker_id": 1,
  "subject": "ceph-bluestore-tool reshard exits 0 when resharding fails",
  "description": "|Kind|wrong exit status; scripts cannot detect the failure|\n|Severity|minor|\n|Config|any|\n|Real-world|*Real tool*: @ceph-bluestore-tool reshard@ on an OSD created by @ceph-osd --mkfs@|\n|Affected|main. Reproduced on origin/main 8e6a13e7a9a|\n\nh3. Summary\n\nThe @reshard@ action (bluestore_tool.cc:1397-1403) prints @error resharding: ...@ but\ndoes not @exit(EXIT_FAILURE)@, so the tool exits 0. This covers:\n* a bad sharding spec: @-EINVAL@ from @prepare_for_reshard()@, returned before the resharding lock is taken, so the DB is unchanged (the repro case);\n* errors after the lock is taken (@reshard_cleanup()@ failure, @-EIO@ writing the sharding definition, RocksDBStore.cc:3775-3923), which may leave the @reshardingXcommencingXlocked@ marker; the OSD then refuses to open while the tool still reported success.\n\nh3. Reproduction\n\n@repro.sh@: @ceph-bluestore-tool --path <osd> --sharding \"m(x) p(3)\" reshard; echo $?@\n\nh3. Observed (origin/main 8e6a13e7a9a)\n\n<pre>\n== current sharding\nm(3) p(3,0-12) O(3,0-13)=block_cache={type=binned_lru} L=min_write_buffer_number_to_merge=32 P=min_write_buffer_number_to_merge=32\n== reshard with an invalid spec (parse error -&gt; -EINVAL)\nerror resharding: (22) Invalid argument\nrc=0\n</pre>\n\nh3. Suggested fix\n\nExit with a non-zero status on any reshard error.\n\nh3. Reproducer files\n\n* \"repro.sh\":https://github.com/cephtools/tools/tree/main/trackers/bluestore-2026-09/20-reshard-failure-exit-zero/repro.sh\n* Test patch for all gtests: \"bluestore-bughunt-tests.patch\":https://github.com/cephtools/tools/tree/main/trackers/bluestore-2026-09/common/patches/bluestore-bughunt-tests.patch\n",
  "custom_fields": [
   {
    "id": 4,
    "value": "3 - minor"
   },
   {
    "id": 13,
    "value": "0"
   }
  ]
 }
}
